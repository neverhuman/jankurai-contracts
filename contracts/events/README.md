# Event Contracts

This directory is the declared source root for queue/event contract schemas in
`agent/boundaries.toml`.

Add versioned event schemas here before generating queue adapter types under the
declared generated contract paths.
